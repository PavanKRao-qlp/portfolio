---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug";
const projects = await getCollection("projects");

const workProjects = projects
  .filter((p) => p.data.type == "work")
  .sort((a, b) => b.data.priority - a.data.priority);
const personalProjects = projects
  .filter((p) => p.data.type != "work")
  .sort((a, b) => b.data.priority - a.data.priority);
---

<BaseLayout title="Projects" sideBarActiveItemID="projects">
  <h1 class="text-4xl font-bold mt-6 mb-8">Projects</h1>
  <section>
    <div>
      <h2 class="text-3xl w-full font-bold mb-5 mt-10">Professional Works</h2>
    </div>
    {
      workProjects.map((post) => (
        <>
          <HorizontalCard
            title={post.data.title}
            img={post.data.heroImage}
            desc={post.data.description}
            company={post.data.company}
            highlights={post.data.highlights}
            url={`${import.meta.env.BASE_URL}projects/${createSlug(post.data.title, post.slug)}`}
            target="_self"
          />
          <div class="divider my-0" />
        </>
      ))
    }
  </section>
  <section>
    <div>
      <h2 class="text-3xl w-full font-bold mb-5 mt-10">Personal Works</h2>
    </div>
    {
      personalProjects.map((post) => (
        <>
          <HorizontalCard
            title={post.data.title}
            img={post.data.heroImage}
            desc={post.data.description}
            highlights={post.data.highlights}
            url={`${import.meta.env.BASE_URL}projects/${createSlug(post.data.title, post.slug)}`}
            target="_self"
          />
          <div class="divider my-0" />
        </>
      ))
    }
  </section>
</BaseLayout>
